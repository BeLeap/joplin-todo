# Remove top Joplin Widget label

- Removed the small `Joplin Widget` label from the top header of the main screen.
- Kept the primary `오늘 할 일` title and status badge layout unchanged.
- Requested for the next task: this was a visual-only header cleanup, so lint remains the main regression check.
